using System;
using System.Collections.Generic;
using System.Drawing;
using System.IO;
using System.Text;
using System.Threading.Tasks;

namespace HalationGhost.WinApps.ImaZip.ZipBookCreator
{
	internal class ImageResearcher
	{
		internal void GetImageInformation()
		{

		}

		//internal async Task<ImageSpecification> ResearchAsync(string filePath)
		//{
		//	return await Task.Run(async () =>
		//	{
		//		await using (var reader = new FileStream(filePath, FileMode.Open, FileAccess.Read, FileShare.Read, 4096, true))
		//		{
		//			using (var img = Image.FromStream(reader, false, false))
		//			{
		//				return new ImageSpecification(img);
		//			}
		//		}
		//	});
		//}
	}
}
